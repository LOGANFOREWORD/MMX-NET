<w>
	<!-- Money-transfer button (xrr_patch_inventory). Position is set at runtime
	     relative to the player money readout so reskins that keep the vanilla
	     anchors keep working. -->
	<xrr_send_money x="0" y="0" width="26" height="16" stretch="1">
		<texture>ui_xrr_money_send</texture>
	</xrr_send_money>
</w>

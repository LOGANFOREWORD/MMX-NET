MMX-Net - feed aggiornamenti (PUBBLICO)

Repo: https://github.com/LOGANFOREWORD/MMX-NET
(pubblica per raw update; write solo owner LOGANFOREWORD)

L amico NON deve clonare la repo: usa installer/zip oppure popup AGGIORNA.
Dopo l install, gli aggiornamenti partono dal launcher senza PAT.

updateFeedUrl (ac_config.json):
https://raw.githubusercontent.com/LOGANFOREWORD/MMX-NET/main/

updateFeedToken: lasciare vuoto
Token forever / PAT nello zip: NON serve.

Install vecchi su MMX-NET: il launcher migra a MMX-NET all avvio.

File feed in root repo:
- ac_version.json
- ac_update_manifest.json
- ac-update.zip
